---
title: Backend Configuration
description: Core environment variables for the FastAPI backend service.
---

# Backend Configuration

The backend is responsible for APIs, persistence, and session management.

## Focus areas

- Database connection via `DATABASE_URL`
- Storage settings such as `S3_*`
- Security and app settings such as secrets, CORS, and logging

Use the production deployment to replace all placeholder secrets and weak defaults.
